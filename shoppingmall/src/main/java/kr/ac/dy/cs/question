package com.shoppingmall.domain;

/*
 * 학번이름: [20251288-김재우]
 * 기능 설명: 상품 Q&A 문의 등록 및 비밀글 설정, 관리자 답변 상태 처리를 위한 게시판 기능 구현
 */

import javax.persistence.*;

@Entity
public class Question {

    @Id @GeneratedValue(strategy = GenerationType.IDENTITY)
    private Long id;

    @ManyToOne(fetch = FetchType.LAZY)
    @JoinColumn(name = "member_id")
    private Member member;

    @ManyToOne(fetch = FetchType.LAZY)
    @JoinColumn(name = "product_id")
    private Product product;

    @Column(nullable = false)
    private String content; // 문의 내용

    private boolean isSecret; // 비밀글 여부 (true/false)

    @Column(length = 50)
    private String status = "WAITING"; // 대기(WAITING) / 완료(COMPLETED)

    @Column(columnDefinition = "TEXT")
    private String answer; // 관리자 답변 내용

    // Getter, Setter 및 비즈니스 로직 생략
}
